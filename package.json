{
  "name": "disinformeter-demonstrator",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "DE-CONSPIRATOR D5.2 — interactive demonstrator of the DisInforMeter Toolkit",
  "scripts": {
    "sync": "node scripts/sync-public.mjs",
    "predev": "npm run sync",
    "prebuild": "npm run sync",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "node --test \"tests/**/*.test.mjs\""
  },
  "dependencies": {
    "astro": "^4.16.18"
  },
  "devDependencies": {
    "@fontsource-variable/montserrat": "^5.2.8",
    "@fontsource/ibm-plex-mono": "^5.1.0"
  }
}
